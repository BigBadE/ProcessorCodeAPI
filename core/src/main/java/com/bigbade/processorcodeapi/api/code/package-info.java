/**
 * Represents Java constructs in memory (instead of source code). Not the same as nodes, because
 * most types aren't editable (the exception being code blocks).
 */
package com.bigbade.processorcodeapi.api.code;
