package com.bigbade.processorcodeapi.api.expressions;

/**
 * A combination of assigning a value and calling an operation on the value stored.
 * Usually used like:
 * String value = "";
 * while((value += "a").length() &lt; 5) {
 */
public interface IAssignmentOperationExpression {
}
