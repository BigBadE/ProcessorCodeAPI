/**
 * Represents Java instructions in memory, acting as an intermediary between source code and bytecode.
 * Expressions return a value, and therefore cannot be added to code blocks unless they are
 * transformed into statements.
 */
package com.bigbade.processorcodeapi.api.expressions;
