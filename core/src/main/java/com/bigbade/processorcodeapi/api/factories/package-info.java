/**
 * Allows the instantiation of implementations of API interfaces that are compiler-specific, either allowing
 * access to internal data not exposed by the default processor API (such as supertypes of TypeElements) and
 * other compiler-specific tasks (like instruction generations).
 */
package com.bigbade.processorcodeapi.api.factories;
