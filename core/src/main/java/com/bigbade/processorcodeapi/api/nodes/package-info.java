/**
 * Nodes represents Java constructs which are both editable (so they are being compiled) and not bytecode instructions
 * (statements and expressions are not nodes).
 */
package com.bigbade.processorcodeapi.api.nodes;
