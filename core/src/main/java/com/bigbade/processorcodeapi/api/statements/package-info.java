/**
 * Represents Java instructions in memory, acting as an intermediary between source code and bytecode.
 * Statements do not return a value, and therefore can be added to code blocks, but not used in values like
 * parameters in methods.
 */
package com.bigbade.processorcodeapi.api.statements;
